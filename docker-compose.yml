services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: velura
    ports:
      - "5432:5432"
    volumes:
      - ./data/db:/var/lib/postgresql/data
